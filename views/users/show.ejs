<h1><%= user.name %></h1>
<img src="<% user.image %>" alt="image of the game">
<p><%= game.body %></p>
<ul>
    <% for(var i = 0; i < game.tags.length; ++i ) {%>
    <li><%= game.tags[i] %></li>
    <% } %>
</ul>
<a href="/users/<%= user.id + 1 %>/edit">edit</a>
