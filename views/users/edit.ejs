<h2>So ye want to edit the user called <%= user.name %>, do ye?</h2>
<% if(user.id) { %>
<form>
  Title:<br>
  <input type="text" name="firstname" value="<%= user.title %>"><br>
  Body:<br>
  <input type="text" name="lastname" value="<%= user.body %>"><br>
  <input type="submit" value="Submit">
</form>
<% } else { %>
  <p>You must be <a href="/login">logged in</a> to change a user!</p>
<% } %>
